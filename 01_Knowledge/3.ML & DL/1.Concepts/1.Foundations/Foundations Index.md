---
tags: [category/ml-dl, index, moc]
---

# Foundations — Index

> **Position in vault**: `3.ML & DL/1.Concepts/1.Foundations/`
> **Purpose**: The vocabulary everything downstream assumes — what a model, a feature, a label, training data, generalization, and the supervised/unsupervised split actually mean before any algorithm is introduced.

## Section Map

| Note | Covers |
|---|---|
| [[Artificial Intelligence]] | AI vs ML vs DL, where each sits relative to the others |
| [[Machine Learning]] | The core definition — learning a function from data rather than hand-coding it |
| [[Model]] | What a model *is* — a parameterized function, not the algorithm that fits it |
| [[Training Data]] | The examples a model learns from, and what makes a dataset fit for that purpose |
| [[Features]] | The inputs a model consumes |
| [[Labels]] | The target a supervised model is trained to predict |
| [[Supervised Learning]] | Learning from labeled examples |
| [[Unsupervised Learning]] | Learning structure without labels |
| [[Generalization]] | Why the real goal is performance on unseen data, not training data |
| [[Inference]] | Using a trained model to produce predictions |

## Reading Order

Machine Learning → Model → Training Data → Features → Labels → Supervised Learning → Unsupervised Learning → Generalization → Inference

## One-line Summary

> This module is the shared vocabulary — every later module assumes these terms are already precise, not approximate.
